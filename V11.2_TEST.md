# v11.2 acceptance test

1. Open Management -> order -> Detalii.
2. The button `ÎNCHIDE COMANDA` must be visible for every non-closed order.
3. Fill `Închisă de` and optional resolution note.
4. Click the button and confirm.
5. The order must become CLOSED and remain in History with all picking/alert data.
6. If already closed, show `Comanda este deja închisă`.

No changes to shortage calculations, configurable thresholds, or v10 persistence behavior.
