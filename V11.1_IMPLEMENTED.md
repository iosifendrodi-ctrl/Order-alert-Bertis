# v11.1 — Close & History REAL

Implemented in UI and persistence:
- Management order details now has real "ÎNCHIDE COMANDA".
- Closure is blocked until an alert verification is resolved.
- Confirmation dialog before closure.
- Closed orders persist `status`, `closed_at`, `closed_by`, `resolution_note`.
- `/history` lists closed orders.
- Dashboard separates Active and Closed.
- Audit trail records ORDER_CLOSED and CLOSE_BLOCKED.
- Existing databases are migrated automatically.
- v10 picking persistence is preserved.
